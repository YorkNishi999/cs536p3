int x;
int y;
void z;
bool w;
bool w1;
int x1_1;
struct struct_a sa1;
struct struct_b {
    int x;
}
struct struct_a {
    int x;
    struct struct_b y;
}
void func () {
    int b;
    int x;
    int y;
}
bool func (bool y, int z) {
    int b;
    int x;
    int y;
}
bool func (bool x, bool y) {
    bool z;
    z = x || y;
    z = x && y;
}
bool func (int x, int y) {
    bool z;
    z = x > y;
    z = x >= y;
    z = x < y;
    z = x <= y;
    z = x == y;
    z = x != y;
    z = x + y;
    z = x - y;
    z = x * y;
    z = x / y;
    z = x + y * x / y;
    z = !x;
    z = !x + y;
    z = !x / y - x;
    ++x;
    --x;
    x = 151;
    y = "string";
    x = 151 + y * z / !x - -z;
    if (x == tru) {
        x = y.z;
    }
    if (x == fls) {
        x = y;
    }
    if (x != y) {
      z = x - y;
    } else {
      z = y - x;
    }
    ret;
    ret z;

    while (!z) {
    z = y;
    ++z;
    --z;
    }

    repeat (z == y) {
    z = x;
    }

    print << x;  /// comment
    receive >> z;  ### comment 
}
int main (int a, int b) {
    int x;
    int y;
    bool z;
    z = !fls;
    if (x == tru) {
        x = 3 + 1;
    } else {
        x = 3 * 1 + 3;
    }
}

int x;
int y;
void z;
bool w;
bool w1;
int x1_1;
struct struct_a sa1;
struct struct_b {
    int x;
}
struct struct_a {
    int x;
    struct struct_b y;
}
void func() {
    int b;
    int x;
    int y;
}
bool func(int z, bool y) {
    int b;
    int x;
    int y;
}
bool func(bool y, bool x) {
    bool z;
    z = (x || y);
    z = (x && y);
}
bool func(int y, int x) {
    bool z;
    z = (x > y);
    z = (x >= y);
    z = (x < y);
    z = (x <= y);
    z = (x == y);
    z = (x != y);
    z = (x+y);
    z = (x-y);
    z = (x*y);
    z = (x/y);
    z = (x+((y*x)/y));
    z = (!x);
    z = ((!x)+y);
    z = (((!x)/y)-x);
    ++x;
    --x;
    x = 151;
    y = "string";
    x = ((151+((y*z)/(!x)))-(-z));
    if ((x == tru)) {
        x = (y.z);
    }
    if ((x == fls)) {
        x = y;
    }
    if ((x != y)) {
        z = (x-y);
    } else {
        z = (y-x);
    }
    ret;
    ret z;
    while((!z)) {
        z = y;
        ++z;
        --z;
    }
    repeat((z == y)) {
        z = x;
    }
    print << x;
    receive >> z;
}
int main(int b, int a) {
    int x;
    int y;
    bool z;
    z = (!fls);
    if ((x == tru)) {
        x = (3+1);
    } else {
        x = ((3*1)+3);
    }
}
int x;
int y;
void z;
bool w;
bool w1;
int x1_1;
struct struct_a sa1;
struct struct_b {
    int x;
}
struct struct_a {
    int x;
    struct struct_b y;
}
void func() {
    int b;
    int x;
    int y;
}
bool func(bool y, int z) {
    int b;
    int x;
    int y;
}
bool func(bool x, bool y) {
    bool z;
    z = (x || y);
    z = (x && y);
}
bool func(int x, int y) {
    bool z;
    z = (x > y);
    z = (x >= y);
    z = (x < y);
    z = (x <= y);
    z = (x == y);
    z = (x != y);
    z = (x+y);
    z = (x-y);
    z = (x*y);
    z = (x/y);
    z = (x+((y*x)/y));
    z = (!x);
    z = ((!x)+y);
    z = (((!x)/y)-x);
    ++x;
    --x;
    x = 151;
    y = "string";
    x = ((151+((y*z)/(!x)))-(-z));
    if ((x == tru)) {
        x = (y.z);
    }
    if ((x == fls)) {
        x = y;
    }
    if ((x != y)) {
        z = (x-y);
    } else {
        z = (y-x);
    }
    ret;
    ret z;
    while((!z)) {
        z = y;
        ++z;
        --z;
    }
    repeat((z == y)) {
        z = x;
    }
    print << x;
    receive >> z;
}
int main(int a, int b) {
    int x;
    int y;
    bool z;
    z = (!fls);
    if ((x == tru)) {
        x = (3+1);
    } else {
        x = ((3*1)+3);
    }
}

struct a {
    int b;
    struct struct2 c;
}
int func(int x, bool u) {
    z = funx2(x, u);
    if (a == b) {
        while (b == c) {
            repeat (c == d) {
                ++e;
            }
        }
    }
    ret z;
}
int x;
int y;
void z;
bool w;
bool w1;
int x1_1;
struct struct_a sa1;
struct struct_b {
    int x;
}
struct struct_a {
    int x;
    struct struct_b y;
}
void func() {
    int b;
    int x;
    int y;
}
bool func(int z, bool y) {
    int b;
    int x;
    int y;
}
bool func(bool y, bool x) {
    bool z;
    z = (x || y);
    z = (x && y);
}
bool func(int y, int x) {
    bool z;
    z = (x > y);
    z = (x >= y);
    z = (x < y);
    z = (x <= y);
    z = (x == y);
    z = (x != y);
    z = (x+y);
    z = (x-y);
    z = (x*y);
    z = (x/y);
    z = (x+((y*x)/y));
    z = (!x);
    z = ((!x)+y);
    z = (((!x)/y)-x);
    ++x;
    --x;
    x = 151;
    y = "string";
    x = ((151+((y*z)/(!x)))-(-z));
    if ((x == tru)) {
        x = (y.z);
    }
    if ((x == fls)) {
        x = y;
    }
    if ((x != y)) {
        z = (x-y);
    } else {
        z = (y-x);
    }
    ret;
    ret z;
    while((!z)) {
        z = y;
        ++z;
        --z;
    }
    repeat((z == y)) {
        z = x;
    }
    print << x;
    receive >> z;
}
int main(int b, int a) {
    int x;
    int y;
    bool z;
    z = (!fls);
    if ((x == tru)) {
        x = (3+1);
    } else {
        x = ((3*1)+3);
    }
}
int x;
int y;
void z;
bool w;
bool w1;
int x1_1;
struct struct_a sa1;
struct struct_b {
    int x;
}
struct struct_a {
    int x;
    struct struct_b y;
}
void func() {
    int b;
    int x;
    int y;
}
bool func(bool y, int z) {
    int b;
    int x;
    int y;
}
bool func(bool x, bool y) {
    bool z;
    z = (x || y);
    z = (x && y);
}
bool func(int x, int y) {
    bool z;
    z = (x > y);
    z = (x >= y);
    z = (x < y);
    z = (x <= y);
    z = (x == y);
    z = (x != y);
    z = (x+y);
    z = (x-y);
    z = (x*y);
    z = (x/y);
    z = (x+((y*x)/y));
    z = (!x);
    z = ((!x)+y);
    z = (((!x)/y)-x);
    ++x;
    --x;
    x = 151;
    y = "string";
    x = ((151+((y*z)/(!x)))-(-z));
    if ((x == tru)) {
        x = (y.z);
    }
    if ((x == fls)) {
        x = y;
    }
    if ((x != y)) {
        z = (x-y);
    } else {
        z = (y-x);
    }
    ret;
    ret z;
    while((!z)) {
        z = y;
        ++z;
        --z;
    }
    repeat((z == y)) {
        z = x;
    }
    print << x;
    receive >> z;
}
int main(int a, int b) {
    int x;
    int y;
    bool z;
    z = (!fls);
    if ((x == tru)) {
        x = (3+1);
    } else {
        x = ((3*1)+3);
    }
}
int x;
int y;
void z;
bool w;
bool w1;
int x1_1;
struct struct_a sa1;
struct struct_b {
    int x;
}
struct struct_a {
    int x;
    struct struct_b y;
}
void func() {
    int b;
    int x;
    int y;
}
bool func(int z, bool y) {
    int b;
    int x;
    int y;
}
bool func(bool y, bool x) {
    bool z;
    z = (x || y);
    z = (x && y);
}
bool func(int y, int x) {
    bool z;
    z = (x > y);
    z = (x >= y);
    z = (x < y);
    z = (x <= y);
    z = (x == y);
    z = (x != y);
    z = (x+y);
    z = (x-y);
    z = (x*y);
    z = (x/y);
    z = (x+((y*x)/y));
    z = (!x);
    z = ((!x)+y);
    z = (((!x)/y)-x);
    ++x;
    --x;
    x = 151;
    y = "string";
    x = ((151+((y*z)/(!x)))-(-z));
    if ((x == tru)) {
        x = (y.z);
    }
    if ((x == fls)) {
        x = y;
    }
    if ((x != y)) {
        z = (x-y);
    } else {
        z = (y-x);
    }
    ret;
    ret z;
    while((!z)) {
        z = y;
        ++z;
        --z;
    }
    repeat((z == y)) {
        z = x;
    }
    print << x;
    receive >> z;
}
int main(int b, int a) {
    int x;
    int y;
    bool z;
    z = (!fls);
    if ((x == tru)) {
        x = (3+1);
    } else {
        x = ((3*1)+3);
    }
}
struct a {
    int b;
    struct struct2 c;
}
int func(bool u, int x) {
    z = funx2(x, u);
    if ((a == b)) {
        while((b == c)) {
            repeat((c == d)) {
                ++e;
            }
        }
    }
    ret z;
}
